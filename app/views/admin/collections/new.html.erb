<div class="container mt-2">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="d-flex justify-content-between align-items-center">
        <div class="text-center flex-grow-1 margin_text">
          <h2>New Collection</h2>
        </div>
        <div>
          <%= link_to 'Go Back', admin_collections_path, class: 'btn color_button_brown fw-semibold' %>
        </div>
      </div>

      <div id="error" class="text-danger mb-3 text-center">
        <% if @collection.errors.any? %>
          <% @collection.errors.full_messages.each do |message| %>
            <p class="fw-semibold"><%= message %></p> 
          <% end %>            
        <% end %>
      </div>

      <%= render 'collections/form', collection: @collection, is_admin: true %>

    </div>
  </div>
</div>